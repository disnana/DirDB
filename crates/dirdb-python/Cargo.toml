[package]
name = "dirdb-python"
version = "0.2.0"
edition = "2021"

[lib]
name = "_native"
crate-type = ["cdylib"]

[dependencies]
dirdb-core = { path = "../dirdb-core" }
pyo3 = { version = "0.23", features = ["extension-module"] }
serde_json = "1"
